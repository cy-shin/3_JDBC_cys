<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<comment>main-query.xml file</comment>
	
	<!-- 키워드로 검색 -->
	<entry key = 'keywordSearch'>
		SELECT CALL_NO, TOPIC, BOOK_NAME, AUTHOR, PUBLISHER, LOC, AVAIL, NVL(TO_CHAR(DUE_DATE,'YYYY-MM-DD'),'-') DUE_DATE
		FROM TB_BOOKS
		JOIN TB_TOPIC USING (TOPIC_CODE)
		JOIN TB_LOCATION USING (LOC_CODE)
		JOIN TB_AVAILABILITY USING (AVAIL_CODE)
		WHERE BOOK_NAME = ?
		OR AUTHOR = ?
		OR PUBLISHER = ?
	</entry>
	
	<entry key = 'login'>
		SELECT MEMBER_ID, MEMBER_NAME, IDENTITY_NAME, LENT_NUM
		FROM TB_MEMBER
		JOIN TB_IDENTITY USING(IDENTITY_CODE)
		WHERE MEMBER_ID = ?
		AND MEMBER_PW = ?
	</entry>
	
</properties>
