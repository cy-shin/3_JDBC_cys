<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<comment>main-query.xml file</comment>
	
	
	
	<entry key = 'login'>
		SELECT USER_ID, USER_NAME, IDENTITY_NAME, LENT_NUM
		FROM TB_USER
		JOIN TB_IDENTITY USING(IDENTITY_CODE)
		WHERE USER_ID = ?
		AND USER_PW = ?
	</entry>
	
</properties>
